# Datalab API key — get one at https://www.datalab.to
# Leave empty to run in mock mode (simulated extractions, no API calls).
DATALAB_API_KEY=

# Local dev:
DATABASE_URL=
# Deployed dev (Neon):
# DATABASE_URL=
